R"==~~~~==(any~~CPP==map==key==delim~~
"any"
\033[32;1many(iterable)\033[0m   Return "True" if any element of the *iterable* is true.  If the
   iterable is empty, return "False".  Equivalent to:

      def any(iterable):
          for element in iterable:
              if element:
                  return True
          return False

~~CPP==new==module==delim~~
)==~~~~=="
