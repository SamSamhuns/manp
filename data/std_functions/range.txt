R"==~~~~==(range~~CPP==map==key==delim~~
"range"
range(start, stop[, step])
   Rather than being a function, "range" is actually an immutable
   sequence type, as documented in Ranges and Sequence Types --- list,
   tuple, range.

~~CPP==new==module==delim~~
)==~~~~=="
